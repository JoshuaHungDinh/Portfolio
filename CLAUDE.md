@AGENTS.md

# Styling

- Always use `.scss` files for styling — never use Tailwind CSS.
- When creating new components or pages, create a corresponding `.scss` file for styles.

# Component Structure

- Each new component must have its own directory (e.g., `src/components/MyComponent/`).
- The directory should contain the `.tsx` file and its corresponding `.scss` stylesheet.
